[metadata]
name = ai-model-risk-management
version = 0.1.0
description = Lightweight Model Risk Management (MRM) toolkit

[options]
packages = find:
include_package_data = True

[options.packages.find]
include = govcli*
exclude =
    models*
    controls*
    artifacts*
    tests*
    docs*
